# 03. OOP & SOLID — System Design

---

## 🎯 설계 목표

> 기능 추가가  
> **기존 코드 수정 없이 확장**되도록 만드는 구조를 설계한다.

---

## 🧨 기존 절차형 구조 문제

```

MainController
├─ if (type == A) { ... }
├─ else if (type == B) { ... }
├─ else if (type == C) { ... }
└─ ...

```

### 문제점
- 기능 추가 시 조건문 폭증
- SRP, OCP, DIP 전부 위반
- 테스트 불가

---

## 🧩 객체지향 구조 설계

```

PaymentService
↑
Payment (interface)
↑
├─ CardPayment
├─ KakaoPayment
└─ NaverPayment

```

### 적용 원칙
- **OCP**: 기능 추가 시 기존 코드 수정 없음
- **DIP**: 구현체가 아니라 인터페이스 의존
- **LSP**: 모든 Payment 구현체는 교체 가능

---

## 🧬 SOLID 반영 구조

| 원칙 | 적용 |
|--|--|
SRP | 클래스 하나 = 책임 하나 |
OCP | 인터페이스 + 다형성 |
LSP | 구현체 교체 가능 |
ISP | 역할별 인터페이스 분리 |
DIP | 상위 정책이 구현체 몰라도 됨 |

---

## 🧠 설계 결론

> 객체지향은 기술이 아니라 **변경을 통제하는 전략**이다.
