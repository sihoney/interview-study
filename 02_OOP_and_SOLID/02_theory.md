# 02. OOP & SOLID — Core Theory

---

## 🧩 객체지향 프로그래밍(OOP)이란?

객체지향 프로그래밍은  
프로그램을 기능 중심이 아니라 **객체 중심**으로 구성하는 방식이다.

객체는 **상태(데이터)** 와 **행위(기능)** 를 가지며,  
각 객체가 자신의 책임을 수행하고 상호작용하면서 시스템을 구성한다.

핵심 질문:
> **"누가 이 일을 담당하는가?"**

---

## 🧬 객체지향 4대 특징

### 1. 캡슐화
데이터와 기능을 하나의 객체로 묶고 외부 접근을 제한하여 안정성 확보

### 2. 상속
기존 클래스를 재사용하여 확장

### 3. 다형성
하나의 인터페이스, 다양한 구현

### 4. 추상화
불필요한 세부사항 숨기고 핵심 구조만 표현

---

## 🧱 SOLID 원칙

### SRP — 단일 책임 원칙
한 클래스는 하나의 책임만 가진다.

### OCP — 개방-폐쇄 원칙
확장에는 열려 있고, 수정에는 닫혀 있어야 한다.

### LSP — 리스코프 치환 원칙
하위 타입은 상위 타입을 대체할 수 있어야 한다.

### ISP — 인터페이스 분리 원칙
하나의 범용 인터페이스보다 여러 개의 역할 인터페이스

### DIP — 의존성 역전 원칙
구현체가 아니라 추상화에 의존

---

## 🔄 오버로딩 & 오버라이딩

- **오버로딩(Overloading)**: 매개변수 타입/개수 다르게 동일 이름 메서드 정의
- **오버라이딩(Override)**: 상위 클래스 메서드 재정의

```
Parent p = new Child();
```

| 구분     | 의미       |
| ------ | -------- |
| 컴파일 타입 | `Parent` |
| 실행 타입  | `Child`  |


> 즉, 컴파일러의 눈에는 Parent, JVM의 눈에는 Child

```
Parent p = new Child();
p.hello(); // 다형성(오버라이딩)
```

- 컴파일 단계: 타입 검사만
- 실행 단계: JVM은 실제 객체 타입(카리키는 실제 객체)을 본다. 

> 그래서 동적 바인딩이 일어난다.
---

## 🔐 접근 제한자

| 제한자 | 범위 |
|--|--|
public | 전체 |
protected | 패키지 + 상속 |
default | 패키지 |
private | 클래스 내부 |

---

## 🧬 추상 클래스

- abstract 키워드 사용
- 추상 메서드 + 일반 메서드 가능
- 관련 클래스 간 코드 공유 목적
- 공통된 정체성과 공통 코드
- 부모가 되고 싶어하는 클래스

---

## 🧩 인터페이스

- 추상 메서드 + 상수만 포함
- 다중 구현 가능
- 관련 없는 클래스 간 기능 강제
- 역할(능력, 규약)을 정의한다.
- 정체성과 무관하게 할 수 있는 일을 강제한다.

---

## 추상 클래스 vs 인터페이스
| 항목  | abstract class  | interface                        |
| --- | --------------- | -------------------------------- |
| 상속  | 단일 상속           | 다중 구현                            |
| 메서드 | 일반 메서드 + 추상 메서드 | 기본적으로 전부 추상 (default, static 제외) |
| 필드  | 인스턴스 변수 가능      | 상수만 가능 (public static final)     |
| 생성자 | 있음              | 없음                               |
| 목적  | **공통 코드 공유**    | **역할 규약 강제**                     |

---

## ⚠ Error vs Exception

| 항목 | Error | Exception |
|--|--|--|
복구 | 불가 | 가능 |
발생 | 런타임 | 컴파일/런타임 |
타입 | Unchecked | Checked / Unchecked |

### Checked Exception vs Unchecked Exception

| 구분                      | 의미               |
| ----------------------- | ---------------- |
| **Checked Exception**   | **컴파일러가 처리 강제**  |
| **Unchecked Exception** | **컴파일러가 관여 안 함** |

### Checked Exception — “반드시 처리해라”

- 처리 안 하면 컴파일 불가
- 외부 환경 요인(파일 없음, 네트워크 끊김, 디스크 문제)이 원인
- 개발자가 예상하고 복구 시도해야 할 문제

### Unchecked Exception — “너 책임이다”

- 처리 안 해도 컴파일 통과
- 왜 풀어놨는가 -> 프로그래머 논리 오류이기 때문
- 코드를 고쳐야 하는 버그지, 런타임에서 복구할 문제가 아니다.

### 메모리 & 실행 흐름 관점
```
Checked   → 외부 요인 → 정상 흐름 일부
Unchecked → 코드 버그 → 즉시 수정 대상
```

### Error는 왜 또 따로인가?

- Error는 JVM 레벨의 치명적 문제
- StackOverflowError, OutOfMemoryError 
- 애플리케이션이 감당 불가 -> 복구 시도 자체가 의미 없음
- 그래서 일반적으로 catch 하지 않는다.

---

## 🧯 try-catch-finally

리소스 생성은 try, 반납은 finally  
실수 가능성 → **try-with-resources 등장**

---

> 이 이론은  
> **"코드를 어떻게 설계해야 안전하고 확장 가능한가"**를 위한 토대다.
