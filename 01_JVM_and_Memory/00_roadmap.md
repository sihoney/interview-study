# 📁 `01_JVM_and_Memory` 과정 계획

> 목표
> **"JVM이 실제 서버를 어떻게 살리고, 어떻게 죽이는지 설명할 수 있는 수준"**

---
 
## 🧩 전체 학습 흐름

```
문제 → 이론 → 구조 설계 → 코드 실험 → 분석 → 면접 답변
```

---

## 🧪 Step 1 — Problem: 서버 장애 시나리오

**01_problem.md**

### 상황 시나리오

> 서비스 트래픽 증가 → 서버 응답 정지
> CPU는 정상, 메모리는 남아 있음
> GC 로그 폭증, Full GC 연속 발생

### 질문

* JVM이 정확히 무엇을 관리하는가?
* 메모리는 어디에 어떻게 저장되는가?
* GC는 언제, 왜 서버를 멈추는가?

---

## 📚 Step 2 — Theory: JVM 핵심 이론 정리

**02_theory.md**

* Java 장단점
* JVM 개념
* Java 실행 과정
* JVM Runtime Data Area 구조
* 메서드 영역 / 힙 / 스택 / PC Register / Native Stack
* Java 데이터 타입 (Primitive / Reference)
* static 변수
* main 메서드 static 이유
* String / StringBuilder / StringBuffer
* new String("") vs ""
* GC 개념, 역할, Mark & Sweep
* final / finally / finalize

---

## 🧱 Step 3 — Design: 메모리 구조 설계

**03_design.md**

### 설계 목표

> 객체 생성 → 참조 → GC → 회수 흐름을
> JVM 메모리 구조로 직접 설명 가능

### 포함 내용

* 객체 생애 주기 다이어그램
* JVM 메모리 구조 맵
* GC Root → Mark → Sweep 설계도
* String Pool 메모리 구조

---

## 🛠 Step 4 — Implementation: 코드 실험

**04_implementation/**

| 파일                  | 실험 내용            |
| ------------------- | ---------------- |
| MemoryTest.java     | Heap Overflow 실험 |
| GcTest.java         | GC 동작 관찰         |
| StringPoolTest.java | String Pool 메모리  |
| StackHeapTest.java  | Stack/Heap 구조    |
| FinalizeTest.java   | finalize 호출 실험   |
| StaticTest.java     | static 메모리 확인    |

---

## 🔬 Step 5 — Analysis: 실험 분석

**05_analysis.md**

* GC 로그 분석
* Heap Dump 분석
* String 메모리 비교 결과
* Stack/Heap 생성 타이밍

---

## 🎤 Step 6 — Interview Output

**06_interview.md**

### 완성 답변

* JVM 구조 설명
* GC 동작 원리
* String 메모리 전략
* static / main 이유 설명
* 서버 장애 대응 답변

---

## 🧭 학습 종료 기준

| 항목     | 기준             |
| ------ | -------------- |
| JVM 구조 | 그림 없이 설명 가능    |
| GC     | 장애 상황 설명 가능    |
| 메모리    | 객체 생애 주기 설명 가능 |
| 면접 답변  | 2분 이내 정리       |
